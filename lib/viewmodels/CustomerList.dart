class CustomersList {
  String id;
  String name;
  double amount;
  double interestRate;
  double penaltyAmount;
  CustomersList(
      {this.id, this.name, this.amount, this.interestRate, this.penaltyAmount});
}
