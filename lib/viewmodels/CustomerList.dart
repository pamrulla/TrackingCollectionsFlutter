class CustomersList {
  String id;
  String name;
  double amount;
  double interestRate;
  int durationType;
  CustomersList(
      {this.id, this.name, this.amount, this.interestRate, this.durationType});
}
